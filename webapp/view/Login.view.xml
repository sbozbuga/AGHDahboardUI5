<mvc:View
    controllerName="ui5.aghd.controller.Login"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m"
    displayBlock="true">
    <App>
        <Page title="AdGuard Home Login">
            <VBox class="sapUiSmallMargin" justifyContent="Center" alignItems="Center" height="100%">
                <Panel width="300px" headerText="Sign In">
                    <content>
                        <VBox>
                            <Label text="Username" labelFor="usernameInput" required="true"/>
                            <Input id="usernameInput" value="{/username}" placeholder="Enter your username" submit=".onLoginPress" liveChange=".onInputChange" maxLength="50" />
                            
                            <Label text="Password" labelFor="passwordInput" class="sapUiSmallMarginTop" required="true"/>
                            <Input id="passwordInput" value="{/password}" type="Password" placeholder="Enter your password" submit=".onLoginPress" liveChange=".onInputChange" maxLength="100" />
                            
                            <Button text="Login" type="Emphasized" press=".onLoginPress" class="sapUiMediumMarginTop" width="100%"/>
                        </VBox>
                    </content>
                </Panel>
            </VBox>
        </Page>
    </App>
</mvc:View>
